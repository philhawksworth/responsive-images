{
  "name": "responsive-images",
  "version": "1.0.0",
  "watch": {
    "build": "src/**/*.js"
  },
  "scripts": {
    "build": "node src/build.js",
    "watch": "npm-watch",
    "start": "npm-watch & live-server dist"
  },
  "license": "ISC",
  "dependencies": {
    "live-server": "^1.2.1",
    "npm-watch": "^0.6.0"
  }
}
